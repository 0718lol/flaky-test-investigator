# 岔路人生：当前状态

更新时间：2026-08-14

## 当前实现

产品位于 `products/parallel-life-simulator/`，是一个由平台托管的零依赖静态 Web 应用。

- `index.html`：应用外壳、导航、弹窗和安全策略。
- `styles.css`：桌面、矮屏桌面和手机响应式布局。
- `app.js`：人物、版本、草稿、时间轴、规则推演、本地存储和导入导出。
- `app.toml`：平台托管配置，使用 `$PORT` 并绑定 `0.0.0.0`。

已实现：

- 人物空间和多人物管理。
- 自我探索与观察人物两种人物类型。
- 七维人物画像及按身份估计。
- 人物档案、五年时间窗口和完整轨迹总览。
- 节点选择、同轴改写、放弃草稿和保存新版本。
- 继续五年、10/15/20 年观察跨度和现实强度设置。
- 已保存版本切换和逐年版本对照。
- 本地历史记录、旧数据迁移、JSON 导入和导出。
- 时间推演返回人物档案、人物档案返回人物空间的连续导航。

## 数据语义

- localStorage 主键：`forked-life-workspace-v2`。
- 旧数据主键：`future-life-state`，加载时自动迁移。
- 未保存草稿只存在于当前页面内存，不进入 localStorage，也不增加版本数量。
- 保存草稿后创建新版本并切换为活动版本。
- 修改人物画像不会修改版本节点中已经保存的状态。

## 已知限制

- 当前推演规则仍以预设事件、场景模板和数值传播为主，人物之间的长期内容可能趋同。
- 结果尚未展示概率、因果依据或假设来源，可信度表达仍不足。
- 场景视觉由本地 CSS 生成，尚无真实 CG 素材。
- 数据只保存在当前浏览器，没有账户、云同步和跨设备恢复。
- 尚未进行目标用户测试，当前判断主要来自产品创建者的连续体验反馈。
- 版本目前不能重命名或删除，人物也没有删除入口。

## 最近反馈

用户认为产品已经比早期原型更像完整产品，但仍不能充分吸引用户兴趣。当前判断是：界面和管理能力已经超过推演内容本身，下一步不应只继续增加页面，而应验证个性化事件、选择张力、现实代价和逐步揭晓是否能形成留存。

这只是问题判断，不代表增强方案已经确认实施。开始下一轮开发前，应先与用户确认优先验证哪一部分。

## 验证基线

最近一次完整验证结果：

- `node --check app.js` 通过。
- `git diff --check` 通过。
- 平台托管首页、`app.js` 和 `styles.css` 返回 HTTP 200。
- 创建人物、编辑画像、按身份估计、五年延伸、节点改写、草稿保存、版本对照、导入和导出均通过浏览器测试。
- 1366x768、1440x900、1512x982 桌面视口无页面级横向溢出；核心推演在常见桌面视口内可完成操作。
- 390x844 手机视口无页面级横向溢出，弹窗操作按钮可达，浮层不会遮挡按钮。

## 最近提交

- `5826773`：增加时间推演、人物档案和人物空间之间的明确返回导航。
- `8ab7175`：完成人物空间、档案、时间推演、版本对照和本地备份的产品化重构。
- `8cc8caa`：产品重命名为“岔路人生”。

## 接手提醒

工作区中存在其他产品的用户改动。后续提交必须限定在 `products/parallel-life-simulator/`，不得清理或带入无关文件。
